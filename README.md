# Medicam Server

FastAPI backend и BLE-провижининг для камеры Medicam на Radxa.

## Закрытый сервисный доступ

Покупатель работает с камерой только в своей локальной сети. Приложение не
регистрируется на сервисном сервере, не знает его адрес и не зависит от него.

Отдельный `medicam-service-tunnel.service` поддерживает исходящее SSH-соединение
с сервером Medicam и публикует SSH камеры только на уникальном loopback-порту
этого сервера. Поэтому на роутере покупателя не нужны проброс портов, белый IP
или другие настройки. Вход по паролю и root-login отключены; камера доверяет
только операторскому ключу, записанному на производстве.

Сервисный канал позволяет единственному оператору Medicam:

- открыть shell камеры по её серийному номеру;
- читать `journalctl` и диагностические файлы;
- передавать файлы через `scp`/`rsync`;
- устанавливать подписанный релиз или выполнить адресное исправление.

Каждая камера получает отдельный ключ туннеля и отдельный обратный порт. Ключ
оператора не хранится на центральном сервере: сервер только пересылает
зашифрованное SSH-соединение с Mac на loopback-порт камеры. Клиент и другие
камеры не получают доступа к этому контуру.

Приватный пакет, созданный fleet-hub для конкретного серийного номера,
устанавливается на Radxa один раз:

```bash
sudo ./scripts/install_service_tunnel.sh /path/to/private-camera-bundle
```

До проверки соединения с Mac нельзя закрывать текущую локальную SSH-сессию.
После успешной проверки пакет с приватным ключом удаляется с промежуточного
носителя. Параметры хранятся в `/etc/medicam/service-tunnel`, а состояние видно
через `systemctl status medicam-service-tunnel.service`.

Каждая плата публикует уникальное mDNS-имя вида
`medicam-XXXXXX.local`, полученное из стабильного device ID и включённое в
TLS-сертификат. Это исключает конфликты общего `nom.local` в сети клиента.

### Автоматический доступ к лабораторной плате

Чтобы автоматизация на доверенном Mac могла выполнять root-диагностику без
повторного ввода пароля, используется отдельная учётная запись
`medicam-maint`. Она получает отдельный Ed25519-ключ и `NOPASSWD: ALL`, но её
SSH-ключ помечен `restrict`: запрещены PTY, port/agent/X11 forwarding и user rc.
Пароль у аккаунта отсутствует, а общий пользователь `radxa` не получает новых
прав.

Установка на лабораторную плату выполняется только после создания ключа на Mac:

```bash
sudo ./scripts/install_lab_maintenance_access.sh /path/to/key.pub
```

Это эквивалент полного root-доступа для владельца приватного ключа. Приватный
ключ нельзя копировать на Radxa, включать в образ, Git или резервные копии без
шифрования. Для серийных камер такой доступ должен устанавливаться только через
производственный процесс и операторский ключ; общий лабораторный ключ нельзя
тиражировать покупателям. Для отзыва доступа нужно удалить
`/etc/sudoers.d/medicam-maint` и аккаунт `medicam-maint` через локальную
административную сессию.

## Подключение устройства

1. Камера постоянно публикует BLE-сервис с именем
   `Medicam-XXXXXX`, в том числе после настройки Wi-Fi.
2. Для первой привязки приложение подтверждает физический 26-символьный код из
   QR через nonce-bound HMAC proof и получает короткую BLE-сессию. Затем оно
   выполняет команды `SCAN_WIFI` и `CONNECT_WIFI` протокола v7.
3. SSID и пароль передаются только как AES-256-GCM `sealed` payload,
   привязанный к `cmd`, `session_id`, `counter` и `request_id`. Owner token не
   передаётся по BLE: приложение и камера независимо выводят его из секретного
   ключа подтверждённой сессии. Камера сохраняет токен только после успешного
   подключения к сети и возвращает IP-адрес устройства.
4. Приложение проверяет сохранённый токен через локальный HTTPS API и дальше
   использует заголовок
   `X-Medicam-Token` для защищённых маршрутов.

BLE остаётся включённым постоянно, поэтому восстановление Wi-Fi не зависит от
загрузочного или временного окна. Уже привязанный iPhone может в любой момент
подтвердить сохранённый в Keychain ключ командой `OWNER_UNLOCK`, не вводя код
повторно. Для нового телефона, полного сброса или утраченного ключа физический
код/QR остаётся обязательным. Неудачные попытки кода и токена ограничены по
частоте, а каждая разрешённая BLE-сессия действует пять минут.

BlueZ настроен с `DiscoverableTimeout=0`: реклама не исчезает по системному
тайм-ауту, но обычный Bluetooth pairing отключён. Если event loop GATT или сама
служба Bluetooth останавливаются, systemd перезапускает BLE-сервис. Клиентские
профили BlueZ для аудио, батареи, клавиатур и системного pairing отключены, чтобы
Linux не инициировал встречное подключение и спаривание с iPhone.

Для разработки и production используется один сценарий привязки. Переменные
окружения не могут отключить API-токен локального API или обязательный физический
QR при первой привязке.

### Получение QR-кода камеры

На Mac QR и печатная этикетка для конкретной камеры создаются одной командой:

```bash
./tool/export_pairing_qr.sh nom.local
```

Вторым аргументом можно указать каталог результата, а вместо `nom.local` —
локальное mDNS-имя, IP или SSH alias нужной камеры. Скрипт вызывает на Radxa
только разрешённую root-owned команду `pairing-info`, не показывает секрет в
аргументах процессов и создаёт локально четыре файла:

- PNG для сканирования с экрана;
- векторный SVG с QR;
- SVG-этикетку 60×40 мм с серийным номером и резервным кодом;
- TXT с резервным кодом и исходной QR-строкой.

Результат сохраняется в `pairing-output/<дата-время>` с правами `0700`, а файлы
получают права `0600`. Это физический ключ владельца камеры: его нельзя
публиковать в Git, отправлять посторонним или оставлять после передачи
единственного экземпляра этикетки покупателю. Зависимости QR-генератора
устанавливаются в локальный `.tools/pairing-qr-venv` и не попадают на Radxa.
QR можно сразу отсканировать в приложении даже у уже настроенного устройства:
защищённый BLE-сервис камеры доступен постоянно.

## Сервисы

- `medicam.service` — HTTP API;
- `medicam-service-tunnel.service` — исходящий закрытый сервисный SSH-туннель;
- `medicam-ble.service` — постоянно доступный защищённый BLE GATT-сервис;
- `medicam-ble-manager.service` — контролирует и при необходимости перезапускает
  BLE GATT-сервис.

Установка/обновление BLE-сервисов:

```bash
./scripts/install_ble_services.sh
```

## Подписанные OTA-обновления

`GET /version` возвращает commit и release backend, версии app/BLE-протоколов,
версию образа Radxa, ревизию USB-камеры и uptime. `/ping` также содержит полный
commit для независимого post-restart healthcheck.

Устройство больше не обновляется с `origin/main`. `GET /update/check` видит
только стабильные теги `medicam-vMAJOR.MINOR.PATCH`, подписанные выделенным
Ed25519 OTA-ключом. Подпись одновременно подтверждает hash commit. Неподписанные
и повреждённые теги игнорируются, downgrade запрещён root-owned trust state.

`POST /update/apply` запускает фоновую установку, а `GET /update/status`
возвращает состояние и прогресс: download, signature verification, install,
restart, healthcheck и rollback. Внешний root-owned активатор ждёт новый backend
60 секунд; при ошибке автоматически восстанавливает предыдущий commit,
зависимости и systemd units. Причина сохраняется и показывается приложением.

Sandbox backend сохраняется полностью: API не вызывает `sudo`. После проверки
и checkout подписанного commit он атомарно создаёт запрос с фиксированной схемой
в `/var/lib/medicam-ota/request.json`. Root-owned `medicam-ota.path` запускает
одноразовый helper, который независимо повторяет проверку подписи/hash,
anti-downgrade и root-owned trust state. Sudoers не содержит OTA-команд.
BLE recovery/reset также не использует sudo. Root-owned BLE manager постоянно
контролирует GATT-сервис и запускает его снова, если тот неактивен. Полный сброс
владельца отправляет атомарный запрос фиксированной systemd-службе на перезапуск
GATT, чтобы очистить предыдущую сессию из памяти. Несекретный recovery-маркер
сохранён для совместимости со старыми версиями приложения, но доступность BLE от
его срока больше не зависит.

Release-процесс и хранение ключа описаны в
[`docs/ota-release.md`](docs/ota-release.md). Подписанный тег создаётся командой:

```bash
./scripts/create_ota_release.sh 1.0.0
```

## Проверка

```bash
medicam-venv/bin/python -m unittest discover -s tests -v
```

## Запись звука

На Linux камера пишет FullHD-видео 30 fps как исходный MJPEG-поток без
перекодирования. Одновременно ALSA сохраняет звук в raw PCM (48 кГц, mono).
После `POST /stop` FFmpeg копирует видеопоток без изменений, кодирует только
звук в AAC 128 кбит/с и синхронизирует начало дорожек. Аудиоинтерфейс открывается
перед UVC-видео, что требуется совмещённой USB-камерой; короткий опережающий
фрагмент звука автоматически отрезается. PCM временно хранится в `/run/medicam`,
чтобы запись FullHD MJPEG на microSD не вызывала переполнение аудиобуфера.
Файлом владеет сервер, поэтому даже аварийный USB-disconnect не удаляет уже
записанный звук и прерванную запись можно финализировать обычным `POST /stop`.
Такой процесс не создаёт дополнительной нагрузки на видеозахват во время
съёмки.

- `GET /audio/devices` — список доступных ALSA-входов и выбранный микрофон;
- `POST /audio/test` — короткая проверка уровня сигнала и перегруза;
- `POST /settings` с `audio_enabled` и `audio_device` — настройка записи;
- `GET /recording/status` — полное состояние записи: фаза, активность камеры и
  микрофона, файл, длительность, текущий размер, свободное место, доступность
  камеры и последняя структурированная ошибка.

Рекомендуемое значение `audio_device=auto`: после подключения USB-микрофона
оно не зависит от номера `/dev/snd/*`. Конкретный вход можно закрепить по его
стабильному ALSA ID, например `plughw:CARD=HD,DEV=0`.

`scripts/install_server_runtime.sh` устанавливает первоначальный systemd
runtime и независимый OTA-активатор. Дальше signed activator синхронизирует
drop-in с `RuntimeDirectory=medicam` и `RuntimeDirectoryPreserve=restart` при
каждом релизе или rollback. PCM в RAM поэтому переживает рестарт сервиса; после
полного отключения питания raw MJPEG всё равно восстанавливается в MP4, но без
утраченного RAM-аудио.

## Надёжность записи

Во время записи сервер атомарно поддерживает
`videos/.recording-state.json`. Watchdog отслеживает неожиданный выход UVC и
ALSA, останавливает оставшийся процесс и переводит запись в `interrupted`.
Повторный `POST /start` не создаёт второй процесс, а `POST /stop` безопасно
повторяется: незавершённый raw-файл собирается в MP4. Если аудио повреждено,
сервер повторяет восстановление без звука; при неудаче raw и PCM не удаляются.
После успешной сборки `ffprobe` возвращает в ответе `quality`: фактические и
ожидаемые кадры, длительность, средний FPS и итоговый признак `healthy`.
Для MP4 используется точный контейнерный `nb_frames`, который читается за
секунды даже у длинной записи; если контейнер не содержит счётчик, сервер
автоматически выполняет полный `-count_frames`.
Timeout финализации и полного `ffprobe` рассчитывается по размеру raw-файла,
поэтому 10- и 30-минутные записи не обрываются по короткому лимиту.

USB-ревизия `32e4:0415` в режиме автоматической экспозиции заявляет FullHD 30
fps, но фактически снижает частоту до 20 fps даже при короткой выбранной
экспозиции. Перед записью сервер поэтому фиксирует текущее значение экспозиции
только для этой ревизии, а после остановки или аварийного recovery возвращает
исходный automatic mode. Это сохраняет яркость стартового кадра и фактические
30 fps; состояние блокировки видно в `recording/status.camera.frame_rate_control`.

OTA-обновление отклоняется с HTTP 409 во время активного захвата или
финализации, чтобы штатное обновление не обрывало съёмку. Для уже прерванного,
но сохранённого raw OTA разрешено — это позволяет установить исправление и
затем повторить безопасное восстановление.

## Предпросмотр камеры

`GET /preview/status` сообщает доступность SD-предпросмотра, а защищённый
HTTPS-поток `/preview/stream` передаёт JPEG-кадры с частотой 10 fps и отображает
их в приложении в размере 640×360.
Каждый кадр предваряется четырёхбайтовой big-endian длиной. Поток использует
тот же API-токен в заголовке `x-medicam-token` и TLS, что и остальные приватные
роуты. Токен никогда не помещается в URL.

В режиме ожидания камера нативно отдаёт MJPEG 1280×720@10 напрямую через V4L2
mmap без FFmpeg; приложение отображает его как 640×360. Во время FullHD-записи основной процесс захвата
остаётся неизменным. Сервер читает растущий raw-файл и без декодирования,
масштабирования или повторного кодирования выбирает 10 из 30 MJPEG-кадров.
Уменьшение FullHD-кадра до 640×360 выполняет iPhone. В памяти платы хранится
только последний JPEG, поэтому медленный клиент не создаёт обратное давление
на запись. Без подписчиков процесс предпросмотра не запускается.

В самом Python-модуле функция отключена по умолчанию. Production unit Radxa
включает её явно для локального preview-контура.
Полная оптимизированная реализация и окно приложения сохранены. На проверенной
аппаратной ревизии её можно включить без новой версии сервера:

```ini
# /etc/medicam/medicam.env
MEDICAM_PREVIEW_ENABLED=1
```

После изменения нужен перезапуск `medicam.service`. Значение `0` явно отключает
предпросмотр; файл подключается unit-файлом как необязательный EnvironmentFile.

Автоматический аппаратный прогон использует именно HTTP-роуты и отдельно
проверяет каждый MP4 через `ffprobe`:

```bash
./scripts/test_recording_long_run.py --durations 60,600,1800
```

По умолчанию создаётся `recording-long-run-report.json` с результатами для
1, 10 и 30 минут: expected/actual frames, avg FPS, длительность, разрешение и
доля доставленных кадров. Тест считается успешным при FullHD, avg FPS не ниже
29.5 и доставке не менее 99.5% ожидаемых кадров.

API запускается на порту `8000`. Публичными остаются только обнаружение и
обезличенный статус; управление камерой требует токен.

## Безопасное выключение

`POST /system/poweroff` требует токен владельца и создаёт одноразовый запрос
для root-owned `medicam-poweroff.path`. Backend не получает `sudo` и не может
передавать произвольную команду: служба выполняет только штатный `poweroff`.
Запрос отклоняется во время записи, восстановления ролика или активного OTA,
чтобы не повредить видео и системное обновление.

## Медиатека

`GET /videos` возвращает быстрый постраничный список. Параметры `page`,
`page_size`, `sort`, `order` и `refresh` позволяют приложению загружать записи
частями, менять сортировку и выполнять pull-to-refresh. Базовые данные файла
возвращаются сразу, а поле `metadata_status=loading` сообщает, что `ffprobe` и
превью ещё готовятся в фоновой задаче. Готовые данные и JPEG-превью хранятся в
атомарном индексе `videos/.media-index.json` и переживают перезапуск backend.

- `GET /videos/{filename}/thumbnail` — авторизованное JPEG-превью;
- `GET /videos/{filename}` — потоковый просмотр с HTTP byte ranges;
- `GET /download/{filename}` — потоковое скачивание оригинального MP4;
- `DELETE /delete/{filename}` — удалить одну запись;
- `POST /videos/delete` с JSON `{"filenames": [...]}` — удалить до 100 записей;
- `DELETE /videos/clear` — очистить медиатеку.

## Управление хранилищем

`GET /storage` возвращает объём накопителя и медиатеки, десять крупнейших
роликов, пороги предупреждения/блокировки и консервативную оценку оставшегося
времени FullHD/30. Оценка использует фактический битрейт готовых роликов и
учитывает двойной объём, необходимый для безопасной сборки MP4 из MJPEG.

Политика автоочистки сохраняется атомарно через `POST /storage/policy`:

- `{"mode":"off"}` — не удалять автоматически;
- `{"mode":"low_space"}` — удалять старые ролики до безопасного остатка;
- `{"mode":"keep_last_gb","value":20}` — хранить последние 20 GB;
- `{"mode":"keep_last_days","value":30}` — хранить последние 30 дней.

`POST /storage/cleanup` с `{"reclaim_gb":5}` вручную освобождает указанный
объём, начиная с самых старых завершённых MP4. Любая очистка запрещена во время
записи. При старте применяется выбранная политика, а при критическом остатке
запись не запускается. Watchdog останавливает текущий захват до исчерпания
резерва, поэтому заполнение карты не повреждает сервис и оставляет место для
финализации.

Удаление блокируется во время записи и финализации с кодом
`recording_in_progress`: это защищает захват кадров от конкурирующей дисковой
нагрузки и исключает гонку с создаваемым MP4.

## Диагностика и поддержка

Все диагностические маршруты требуют действующий `X-Medicam-Token`:

- `GET /diagnostics/health` — единый снимок Wi-Fi/IP, накопителя, камеры,
  микрофона, записи, backend/BLE/OTA, systemd-служб, температуры и USB-цепочки;
- `POST /diagnostics/self-test` — 30 кадров FullHD/30 без добавления ролика в
  медиатеку, секундный тест микрофона, запись/чтение/fsync накопителя и проверка
  локальной сети/шлюза;
- `GET /diagnostics/bundle` — потоковый ZIP с health и ограниченными хвостами
  backend, FFmpeg, BLE и OTA-журналов.

Self-test и старт записи используют общий аппаратный lock: UVC/ALSA никогда не
открываются двумя операциями одновременно. Тестовый MJPEG и файл проверки
накопителя всегда удаляются; последний результат атомарно сохраняется в
`/var/lib/medicam/last-self-test.json` с режимом `0600`.

Перед упаковкой архива сервер рекурсивно скрывает SSID, удаляет текущие и
помеченные старые API-токены, пароли, PSK, pairing secrets, заголовки
Authorization и любые PEM private keys. На каждый источник журнала действует
лимит 512 KiB, архив не сохраняется на Radxa.

Signed OTA устанавливает `deploy/udev/99-medicam-usb-power.rules`: для камеры
`eba4:6579` и поддерживаемого VIA-хаба `2109:2817/0817` выставляется
`power/control=on`. Это исключает runtime autosuspend как программную причину
одновременного исчезновения UVC и USB Audio. Диагностика показывает фактическую
скорость USB, цепочку хаба, режим питания и найденные в `ffmpeg.log` ошибки
отключения. Для коммерческого устройства всё равно предпочтителен короткий
прямой OTG-адаптер либо качественный хаб с отдельным питанием.
